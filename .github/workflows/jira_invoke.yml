name: Jira Service Request Handler

on:
  repository_dispatch:
    types: [jira-issue-created]

jobs:
  handle:
    runs-on: ubuntu-latest
    steps:
      - name: Print Jira Issue Info
        run: |
          echo "Triggered by Jira issue: ${{ github.event.client_payload.issue_summary }}"
